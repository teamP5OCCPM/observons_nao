<div class="card justify-content-around">
    <div class="card-body d-flex flex-wrap">
        <div class="col-md-4">
            <i class="fa fa-user fa-4x" aria-hidden="true"></i>
        </div>
        <div class="col-md-8">
            {{ comment.message }} <br/>
        </div>

        <div class="col-md-12">
            <p>{{ comment.author }} | {{ comment.createdAt|date('d/m/Y')  }}</p>
            <p class="pull-right">
                <a href="#" class="btn btn-secondary btn-sm cta-small">répondre</a>
                <a href="#" class="btn btn-secondary btn-sm cta-small">signaler</a>
            </p>
        </div>


    </div>
</div>

<div style="margin-left: 50px;">
{% if comment.children is not null %}
    {% for comment in comment.children %}
        {{  include('blog/comment.html.twig') }}
    {% endfor %}
{% endif %}
</div>