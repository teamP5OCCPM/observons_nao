<div class="card justify-content-around">
    <div class="card-body d-flex flex-wrap">
        <div class="row">
            <div class="col-12">
                <i class="fa fa-user fa-4x" aria-hidden="true"></i>
            </div>
        </div>
        <div class="row">
            <div class="col-12">
                {{ comment.message }} <br/>
            </div>
        </div>
    </div>
    <div class="card-footer">
        <p>
            {{ comment.author }} | {{ comment.createdAt|date('d/m/Y')  }}
            <a href="#" class="btn btn-secondary btn-sm cta-small pull-right">répondre</a>
            <a href="#" class="btn btn-secondary btn-sm cta-small pull-right mr-3">signaler</a>
        </p>

    </div>
</div>

<div style="margin-left: 50px;">
{% if comment.children is not null %}
    {% for comment in comment.children %}
        {{  include('blog/comment.html.twig') }}
    {% endfor %}
{% endif %}
</div>