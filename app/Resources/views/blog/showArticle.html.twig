{% extends 'base.html.twig' %}

{% block title %}
    {{ article.title }} - {{ parent() }}
{% endblock %}

{% block body %}
<div class="container mt70">
    <h1>{{ article.title }}</h1>
    <p>{{ article.content }}</p>
</div>
{% endblock %}

